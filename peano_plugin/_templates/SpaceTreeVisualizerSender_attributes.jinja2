  int _treeNumber;

  /// Accumulates CellRecord bytes during one traversal sweep
  std::vector<uint8_t> _cellBatch;

  /// Step index for the current traversal round (set in prepareTraversal,
  /// incremented in unprepareTraversal — both are static/once-per-rank).
  static std::atomic<int> _currentStep;

  static tarch::logging::Log _log;
